<h1>Listing places</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Lat</th>
    <th>Long</th>
    <th>URL</th>
    <th>Active</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @places.each do |place| %>
  <tr>
    <td><%= place.name %></td>
    <td><%= place.address %></td>
    <td><%= place.latitude %></td>
    <td><%= place.longitude %></td>
    <td><%= link_to place.url %></td>
    <td><%= place.active %></td>
    <td><%= link_to 'Show', place %></td>
    <td><%= link_to 'Edit', edit_place_path(place) %></td>
    <td><%= link_to 'Destroy', place, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<table>
  <tr>
    <th>Name</th>
    <th>Address</th>
  </tr>

<% Place.near('Fairland, IN, US', 2000).each do |place| %>
  <tr>
    <td><%= place.name %></td>
    <td><%= place.address %></td>
  </tr>
<% end %>
</table>

<br />
<%= link_to 'New Place', new_place_path %>
